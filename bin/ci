#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

echo "==> gofmt"
GOFMT_OUT=$(gofmt -l .)
if [ -n "$GOFMT_OUT" ]; then
  echo "Files need formatting:"
  echo "$GOFMT_OUT"
  exit 1
fi

echo "==> go vet"
go vet ./...

echo "==> golangci-lint"
golangci-lint run

echo "==> go test"
go test ./... -race -coverprofile=coverage.out -covermode=atomic

echo "==> go build"
go build ./...

echo "==> CI passed"
